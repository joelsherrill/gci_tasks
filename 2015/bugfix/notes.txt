The bugs are hard-coded into bugfix.py

I generated the bug lists by using queries on Trac and manually
copying the ticket numbers. Probably this can be automated better.

So far, I have made the bugfix lists for the following:
milestone=4.9.5, not closed
miletone=4.11, not closed

